# Credentials — never commit
terraform.tfvars

# Terraform working directory
.terraform/
.terraform.lock.hcl

# State files — will move to R2 backend
terraform.tfstate
terraform.tfstate.backup

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json
